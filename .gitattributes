# Check files out with LF everywhere, including on Windows. Docker builds copy
# the working tree, and CRLF breaks shell scripts and configs inside Linux images.
* text=auto eol=lf

*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
